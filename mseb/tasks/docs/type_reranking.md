# Reranking

Given a set of candidate answers, reranking tasks evaluate a model's ability to
re-order them such that the most relevant results appear at the top. This is
often used to refine the output of a primary retrieval system.
