# Clustering

Clustering tasks evaluate how well sound embeddings group together based on
semantic or acoustic similarity without explicit labels during the grouping
process. This tests the inherent structure and separability of the embedding
space.
